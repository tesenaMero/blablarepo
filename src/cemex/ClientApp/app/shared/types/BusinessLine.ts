/*
 * {
    TODO Example interface data
  }
*/

// struncture does not have ID

interface BusinessLine {
  businessLineId: number;
  businessLineCode: string;
  businessLineDesc: string;
  divisionCode: string;
  divisionDesc: string;
}

export default BusinessLine;
